# Art Wall — 領域知識索引

> **載入方式**：先讀此索引，再依需求載入對應檔案。不需要的不載入。

---

## 領域知識檔案

**目前沒有。** 現有知識都已在下列常駐文件中，不另開檔重複。

原索引列的 `maze-algorithm.md`（迷宮生成／迷霧／BFS 尋路）與 `achievements.md`
（成就解鎖）兩檔從未建立，且對應功能已於 MR-008 轉向作品牆時全數移除，故一併刪除。

**何時該新增知識檔**：某個主題的「為什麼這樣做」超出程式碼註解能承載的篇幅，
且不屬於架構全貌（→ `ARCHITECTURE.md`）也不屬於決策（→ `prepare.md`）時。

---

## 快速判斷

- 需要架構全貌（資料層／持久層／狀態層／視圖層、兩層主題系統） → `@../../docs/ARCHITECTURE.md`
- 需要部署與 `vite base` 脈絡 → `@../../docs/DEPLOYMENT.md`
- 需要測試分層、範圍與版本鎖 → `@../../docs/TESTING.md`
- 需要決策脈絡（為什麼這樣改） → `@../../prepare.md`
- 需要 scopes、開發指令、文件觸發詞 → `@../../CLAUDE.md`
